#服务名
spring:
  application:
    name: user-service

#服务端口
server:
  port: 5200

#注册中心地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:5000/eureka/

#feign熔断
feign:
  hystrix:
    enabled: true